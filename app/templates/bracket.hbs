<div class="container">
  {{!-- header, bracket name, etc --}}
  <h1>{{model.bracket.name}}{{#if model.bracket.isOpen}} - {{currentRound.name}}{{/if}}</h1>

  <div class="">
  {{#if isOwner}}
    <button {{action 'nextStatus'}} class="btn btn-primary" disabled={{if model.bracket.isClosed 'true'}}>{{roundStatusButtonLabel}}</button>
  {{/if}}
  </div>

  {{bracket-viewer bracket=model.bracket rounds=model.bracket.rounds isOwner=isOwner gotoMatch="gotoMatch"}}

</div>
