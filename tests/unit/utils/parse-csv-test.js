import parseCsv from 'bracketize/utils/parse-csv';
import { module, test } from 'qunit';

module('Unit | Utility | parse csv');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseCsv();
  assert.ok(result);
});
