import randomWords from 'bracketize/utils/random-words';
import { module, test } from 'qunit';

module('Unit | Utility | random words');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomWords();
  assert.ok(result);
});
