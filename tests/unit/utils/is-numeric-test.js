import isNumericJs from 'bracketize/utils/is-numeric';
import { module, test } from 'qunit';

module('Unit | Utility | is numeric');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isNumericJs();
  assert.ok(result);
});
